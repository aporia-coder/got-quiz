import React from "react";

const Landing = ({ Title }) => {
  const handleSubmit = e => {};

  return (
    <section>
      <div className="landing">{Title}</div>
    </section>
  );
};

export default Landing;
